server {
    listen 80;
    listen [::]:80;

    server_name bookitout.site www.bookitout.site; # 여기에 실제 도메인 이름을 입력하세요

    # Certbot 챌린지 요청을 위한 설정
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # React/Next.js 프론트엔드 애플리케이션을 서비스하는 설정
    # bookitout-front의 Dockerfile이 빌드된 파일을 /usr/share/nginx/html에 복사한다고 가정합니다.
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html; # React Router와 같은 SPA를 위한 설정
    }

    # API 요청을 백엔드로 프록시 (필요시 추가)
    # location /api/ {
    #     proxy_pass http://backend:8000;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # Flask 크롤러 API 요청을 프록시 (필요시 추가)
    # location /crawler/ {
    #     proxy_pass http://flask:5000; # Flask 컨테이너의 내부 포트
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
} 